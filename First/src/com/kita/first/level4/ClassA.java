package com.kita.first.level4;

public class ClassA {
	// 부모타입은 자식 객체 주소값을 저장할 수 있다.(가리킬수 있다.)
	// 자식타입은 부모 객체 주소값을 저장할 수 없다.
	// 타입은 메소드 호출 할수있냐없냐 결정(자기가 알고 있는 메소드만 호출가능)
	// 호출 했을시에는 객체 기준으로 실행이 된다.
	public ClassA() {
		super();
	}
}
